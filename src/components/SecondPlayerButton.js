import * as React from 'react';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';

export default function SecondPlayerButton() {
  return (
    <Button color="primary" variant="contained" style={{ position:"absolute", left: "69%", bottom: "40%" }}>Aukera hautatu</Button>

  );
}